<?php
# Walkthrough scenario: run the Jethro installer against an initially empty
# database, then build up the reference instance through the UI.
# functest_databases_setup (process-compose.yml) drops and recreates
# jethro_functest_walkthrough as an empty database on every service start.
define('DB_DATABASE', 'jethro_functest_walkthrough');
